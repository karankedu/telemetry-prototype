# Absolute host path to this directory — used by the Airflow DockerOperator
# to bind-mount spark jobs and Delta Lake files into the batch Spark container.
# Use forward slashes on Windows (Docker Desktop translates them).
HOST_PROJECT_DIR=C:/path/to/02-distributed-kappa-stream-lakehouse
